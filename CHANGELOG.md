# Changelog

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の
思想（新しいバージョンを先頭に追記する）に倣い、バージョンごとの変更点を
新しい順に記録する。見出しは本家の `## [X.Y.Z] - 日付` 形式ではなく、
常に `## vX.Y.Z` 固定とする（ブラケットや日付を付けない）。これは
release.ymlが起動時に`$GITHUB_REF_NAME`と完全一致する見出しを機械的に
抽出するための制約であり、崩さないこと。

各バージョンの見出し（`## vX.Y.Z`）の下に、該当する分類だけを
「新機能」「改善」「不具合修正」「内部改善」の順で箇条書きする。
Breaking Changeがある場合は、分類より前に `### Breaking Changes` として
記載し、必要な移行手順を添える。

## v3.5.0

### 新機能
- GitHub Releaseに、AIエージェント向けの操作Skillアーカイブ `wbsgen-skill.zip` を追加しました。CodexやClaude Codeなどで、配布zipappを安全な手順で操作できます。

### 改善
- 配布マニュアルの章構造と導入手順を整理し、PC・iPad・iPhoneを含む画面幅で読みやすくしました。

## v3.4.0

### 新機能
- `wbsgen describe` を追加し、AIエージェントやツール連携がコマンド、引数、副作用、dry-run可否をJSONで取得できるようになりました。
- 生成HTMLで、iPadでは行・日付・ガントセルをタップして複数のハイライトを追加・解除できるようになりました。PCでは⌘/Ctrl+クリックに加え、ダブルクリックでも同じ操作ができます。

### 改善
- 引数なしで `wbsgen` を実行したときに、`wbsgen --help` を案内するようにしました。

### 内部改善
- AIエージェントによるCLI利用の探索負荷とエラー回復を評価する基盤を整備しました。

## v3.3.1

### 不具合修正
- 検索に直接一致した親タスクの右ペイン行で、検索ハイライトが表示されない問題を修正しました。

### 内部改善
- 内部実装を責務別モジュールへリファクタリングし、更新処理とHTML更新CLIの保守性を改善しました。既存の公開API互換性は維持しています。

## v3.3.0

### Breaking Changes
- `task show --include-generated` を `task show --complement` へ改名しました。旧オプションは使えません。既存のスクリプトや手順では `--include-generated` を `--complement` に置き換えてください。

### 新機能
- JSONまたは生成済みHTMLから、Markdown・CSV形式でWBSをエクスポートできるようになりました。Markdownは `export markdown` または `export md`、CSVは `export csv` を使います。
- 生成HTMLをブラウザから印刷・PDF保存できるようになりました。印刷時は操作UIを除き、WBS表とガントを出力します。
- `task add` でIDを省略したとき、`--parent-id` を指定すれば子タスクIDを、指定しなければ最上位IDを自動採番できるようになりました。
- 検索で空白区切りの複数キーワードをAND条件として指定できるようになりました。先頭に `-` を付けると除外条件になります。

### 改善
- task関連コマンドのヘルプにWBS ID形式と利用例を追加し、`task show --complement` で計算値を補完表示できるようにしました。

### 内部改善
- 固定配布契約、MANUAL検証、探索QAの品質確認を強化しました。

## v3.2.1

### 改善
- ガントバー（計画線・進捗線）の終端デザインを、角丸から四角形へ変更しました。
- README.mdを刷新し、コンセプト・動作環境・スクリーンショット・クイックスタートなどを追加しました。

### 不具合修正
- iPad横向きなど画面幅が狭い環境で、ツールバーの要素がクリップされて操作できなくなる問題を修正しました。

## v3.2.0

### 新機能
- `holiday import-gov` を追加し、内閣府互換CSVから休日を生成済みHTMLへ取り込めるようにしました。
- 既定URLの取得に加え、HTTPS URLの明示指定と、ローカルCSVによるオフライン取込をサポートしました。
- 取込前の差分確認（`--dry-run`）と、既定URL／URL明示の実通信確認用profileを追加しました。

## v3.1.1

### 新機能
- タスク名・コメント・担当者・Issue番号を対象に、フィルタ／ハイライト表示できる検索機能を追加しました。
- 現在の検索・表示設定を再現する共有リンクをクリップボードへコピーできるようにしました。

### 改善
- 検索ドロワー、状態ピル、表示設定メニューを承認済みデザインへ整合しました。

### 不具合修正
- 検索結果が0件のとき右ペインに空行が残る問題を修正しました。
- 分析タブで表示列の状態が漏れ、左右ペイン境界が崩れる問題を修正しました。

### 内部改善
- リリース時の品質検証をCIへ一元化し、配布物の組み立てと公開の処理を分離しました。

## v3.1.0

### 新機能
- タスク名・コメント・担当者・Issue番号を対象に、フィルタ／ハイライト表示できる検索機能を追加しました。
- 現在の検索・表示設定を再現する共有リンクをクリップボードへコピーできるようにしました。

### 改善
- 検索ドロワー、状態ピル、表示設定メニューを承認済みデザインへ整合しました。

### 不具合修正
- 検索結果が0件のとき右ペインに空行が残る問題を修正しました。
- 分析タブで表示列の状態が漏れ、左右ペイン境界が崩れる問題を修正しました。

### 内部改善
- テストコードをpytest記法へ統一しました。

## v3.0.0

### Breaking Changes
- 入力JSONの `display` の形式が変わりました。旧形式 `"display": {"columns": [...], "layers": [...]}` は使えなくなり、`"display": {"standard": {"columns": {"visible": [...]}}, "layers": {"visible": [...]}}` への移行が必要です。既存のJSONは `display.columns` を `display.standard.columns.visible` へ、`display.layers` を `display.layers.visible` へ書き換えてください。
- `wbsgen display update` の `--columns` / `--layers` オプションは廃止されました。`wbsgen display update standard --visible ...` / `wbsgen display update layers --visible ...` を使ってください。

### 新機能
- WBS表の幅可変列（タスク名・担当者・コメント）の初期幅を、生成元JSONの `display.standard.columns.width` でpx単位で指定できるようになりました。
- WBS表の列順（標準ビュー・分析ビュー）を、生成元JSONの `display.standard.columns.order` / `display.analysis.columns.order` で指定できるようになりました。生成HTMLの☰メニューからも一時的に並べ替えられ、標準・分析タブで表示状態が同期します。
- `wbsgen display update` から、標準ビューの列表示・列幅・列順、分析ビューの列順、レイヤー表示を個別に更新・クリアできるようになりました（`display update standard` / `display update analysis` / `display update layers`）。あわせて生成HTMLのURLクエリ（`standardOrder` / `analysisOrder` / `columnWidths`）で列順・列幅を一時的に指定できるようになりました。

### 不具合修正
- WBS表ヘッダーの幅が狭いときに列名が省略表示（ellipsis）されない不具合と、それに伴うセンタリング崩れを修正しました。
- コメント列のデフォルト幅を244pxから220pxに是正しました（担当者列追加以降、意図せず広がっていた表示を修正）。

## v2.2.0

### 新機能
- タスクに担当者を設定できるようになりました。タスクJSONの `assignee` フィールド、`task add` / `task update` CLIの `--assignee` オプション（`--clear assignee` にも対応）、WBS表の「担当者」列（タスク名の直後に表示し、標準・分析どちらの表示でも確認できます）、XLSXエクスポートで担当者を扱えます。WBS表の担当者列は、画面左上の☰メニューやURLクエリパラメータから表示・非表示を切り替えられます。

## v2.1.0

### 新機能
- 生成HTMLに、生成日時・バージョン・GitHubリポジトリ・マニュアルへの導線を表示するようにしました。生成日時・バージョンはWBS表下部のフッターに常時表示され、GitHubリポジトリ・マニュアルへのリンクは画面左上の☰メニュー内「WBS-GENについて」から確認できます。

## v2.0.1

### 改善
- リリースノートを、PRタイトルの一覧ではなく、利用者向けに変更内容を要約した
  内容へ刷新しました。新機能・改善・不具合修正・内部改善に分類し、破壊的変更
  がある場合は明示します。
- GitHub Releaseに、動作確認用のサンプル入力ファイル `wbsgen-sample.json` を
  同梱するようにしました。`wbsgen.pyz` だけをダウンロードした場合でも、
  `python3 wbsgen.pyz generate wbsgen-sample.json -o sample.html` ですぐに
  生成結果を試せます。

## v2.0.0

### Breaking Changes
- 生成HTMLが正本（メインで日々更新するファイル）になりました。以前は入力JSONを都度編集して都度HTMLを作り直す運用でしたが、v2では一度HTMLを生成した後は、そのHTMLに対して直接タスクや進捗を更新でき、元のJSONを保持し続ける必要がなくなりました。JSON運用（一括編集・スクリプト処理・Git管理向け）とHTML運用（手元やファイルサーバーでの日々の更新向け）を明確に分離した設計です。
- CLIコマンド体系が変わりました。主な対応表は以下の通りです。
  | v1 | v2 |
  |---|---|
  | `render 入力JSON -o 出力HTML` | `generate 入力JSON -o 出力HTML` |
  | `restore-source 入力HTML` | `export json 入力HTML` |
  | `task add/update/remove`、`project update`、`milestone add/update/remove`、`holiday add/update/remove` の対象がJSONファイル | 同名コマンドの対象がHTMLファイルに変更（先に`generate`でHTMLを作成してから更新する） |
  | 更新系コマンドの`--render 出力HTML`オプション | 廃止（更新のたびにHTML正本自体が自動で再生成されます） |
  | `validate --holidays` / `export xlsx --holidays` | 廃止 |
  | `render --holidays`（休日の完全置換） | `generate --holidays`（日付単位でマージし、同日は外部側を優先） |
  | HTMLヘッダー上部の「参照JSON: input.json」表示 | ハンバーガーメニューの「データ」>「JSON」からエクスポート |
- 旧コマンド名・旧引数の互換エイリアスは用意していません。既存の運用スクリプトは上記対応表に沿った書き換えが必要です。
- 既存のJSONファイル・既存の生成HTMLはそのまま利用できます。専用の移行コマンドはなく、`generate`（JSON側）または最初の更新操作・`refresh`（HTML側）を実行すると、現行のメタデータ・HTML構造へ自動的に移行されます。`_wbsgen`メタデータを持たない旧JSON・旧HTMLもエラーにはなりません。

### 新機能
- HTML正本を直接更新する新コマンド群を追加しました。`project`・`task`・`milestone`・`holiday`に加え、表示設定（表示する列やガントの表示レイヤーなど）を更新する`display`コマンドを新設しています。更新はいずれもHTML全体を検証・再生成したうえでアトミックに保存するため、途中失敗で元のファイルが壊れることはありません。`--dry-run`で保存前に変更内容（JSON差分）だけを確認できます。
- `refresh`コマンドを追加しました。埋め込みJSONはそのままに、最新のCLIでHTMLの表示部分（HTML/CSS/JS）だけを作り直せます。
- `export json`コマンドを追加しました。HTML正本に埋め込まれているJSONを取り出し、標準出力またはファイルへ書き出せます（Git管理やバックアップ用途を想定）。
- `version`コマンドを追加しました。CLI自体のバージョンに加え、対象のJSON・HTMLを最後に書き込んだ生成元バージョン（`_wbsgen.generatorVersion`）を確認できます。Release版のzipappはタグのバージョンを、開発版は`development`を表示します。
- ハンバーガーメニューから、HTML正本の埋め込みJSONを`wbsgen-source.json`としてダウンロードできるようになりました。
- ガントチャート上にマイルストーン（日付＋名前）を帯・縦線で表示できるようになり、`milestone`コマンドで追加・更新・削除・参照できます。

### 改善
- 外部休日ファイルの取り込み方法を、完全置換から日付単位のマージ（同日は外部側を優先）に統一しました。`generate --holidays`と`holiday merge --from`で共通の規則を使います。
- `validate`・`export xlsx`はJSON・HTMLのどちらの入力も受け付けるようになりました。

### 内部改善
- README・配布用マニュアル（MANUAL.html）・テスト仕様・リリース手順書をv2のJSON/HTML運用に合わせて全面的に更新しました。
- マニュアルの図版を、手描き相当の説明図から実際のHTML/スクリーンショットベースの図版に置き換え、専用の生成ツール（`tools/render_manual_figures.py`）を追加しました。

## v1.2.0

### 新機能
- XLSX（Excel）出力のガントシートにマイルストーンを表示できるようになりました。日付ヘッダーの直下に「マイルストーン」専用の行が追加され、該当日のセルに `◆名前` の形式で表示されます（同じ日に複数のマイルストーンがある場合は読点区切りで並記）。あわせて、そのマイルストーン日には基準日の縦線とは別の色（赤系）の縦線がガント表全体に引かれるため、HTML表示と同様にXLSX上でも重要な日付を視覚的に把握できます。

### 不具合修正
- XLSXガントシートで、コメント列に長い文字列を入力すると隣接する日付グリッドの列にはみ出して表示されてしまう不具合を修正しました。コメント列の直後に区切り用の列が追加され、はみ出しが発生しなくなりました。

## v1.1.0

### 新機能
- マイルストーン表示に対応しました。入力JSONのトップレベルに `milestones` 配列を追加すると、ガントチャート上に固定表示のマイルストーン帯（◆マーカー・名前ピル・縦線）が表示されます。日付が近接するマイルストーンはラベルが重ならないよう自動的に段組みされ、既存のレイヤー切り替え機能（画面上のトグル、`display.layers`、URLの `?hideLayers=`）で表示・非表示を切り替えられます。`wbsgen milestone add/update/remove/show` サブコマンド（`--dry-run` 対応）で正本JSONのマイルストーンを直接編集でき、`wbsgen template` の出力にも雛形が含まれます。なお、XLSXエクスポートへの反映は今回のスコープ外で、別途対応予定です。
- 休日を管理するCLIサブコマンド `wbsgen holiday add/update/remove/show` を追加しました。休日の追加・変更・削除・一覧確認を、正本JSONを直接編集することなくコマンドから行えます。
- `holiday`・`task` などの正本JSON更新系コマンドに `--render OUTPUT_HTML` オプションを追加しました。JSON更新と同時にHTMLを再生成でき、これまで手動で行っていた「JSON更新→再生成」の2手順を1コマンドにまとめられます（`--dry-run` とは同時指定不可）。出力先を入力JSONと同一ファイルに指定した場合の破損防止のほか、HTMLの書き込みだけが失敗した場合でもJSON側の更新内容は保持したまま終了コード1で終了するなど、安全に運用できるよう配慮されています。

### 内部改善
- マニュアル（MANUAL.html）のコマンドリファレンスを、各コマンドの説明だけで完結して読める構成に整理し、検証コード一覧を「レベル・コード・発生条件」の一覧表に統一しました。

## v1.0.1

### 改善
- 配布物に含まれる利用者向けマニュアル（MANUAL.html）を全面改訂した。導入から日々の更新までの流れを説明するGUIDE編（です・ます調）と、コマンド仕様・入力JSON仕様・検証コード一覧・Excelエクスポート仕様などをまとめたREFERENCE編（常体）の2部構成に再編し、データフロー図や画面レイアウト図、コードのコピーボタン、コマンド／検証コードの絞り込み検索、OS別（macOS・Linux／Windows）のコマンド表示切り替えなど、実際に読みながら操作する際に役立つ機能を追加した。あわせて `validate --json` の出力例や `task remove` の説明漏れ、`task show --direct` の記載など、マニュアル内の事実誤りも修正した。

## v1.0.0

WBS-GENの最初の安定版リリースです。JSON形式のWBS/ガント定義から、閲覧用HTML・Excel(XLSX)を生成するCLIツールとして、基本的な機能一式を実装しました。

### 新機能

- **CLIの基本ワークフロー**: JSON入力の検証、WBSツリー構築、稼働日ベースの日程・進捗計算、静的HTML生成という一連の基本機能を実装しました。
- **WBS表とSVGガントチャートの生成**: WBS表（ID・タスク名・計画/実績期間・進捗・Issue・コメントなど）と、計画バー・進捗塗り・実績線・イナズマ線（進捗の遅れ/先行を示す折れ線）を描画するSVGガントチャートを、1枚の静的HTMLとして出力します。
- **生成HTML上の閲覧操作**: 行の折りたたみ、列（計画/実績/進捗/期待/Issue/コメント）の表示・非表示切り替え、表示レイヤー（イナズマ線・実績線・ハイライト・ツールチップ）のON/OFF切り替え、行・日付列のホバー時クロスハイライト、タスクバーやセルへのホバー時ツールチップに対応しました。表示のデフォルト状態は入力JSON（`display`設定）やURLクエリで指定できます。
- **遅延・期待進捗の可視化**: 基準日時点の期待進捗率を表示する「期待」列、遅延タスクの計画バー強調表示、表示範囲外にはみ出す計画期間のクリップ表示を追加しました。
- **標準/分析タブでの進捗分析**: WBS表に「分析」タブを追加し、基準日時点の進捗差分・遅れ営業日換算・残り必要ペースを確認できるようにしました。
- **祝日・休日を考慮した営業日計算**: 入力JSONまたは`--holidays`オプションで祝日・会社休日・任意休日を指定でき、計画終了日・期待進捗・遅延判定に反映されます。生成HTML側には休日一覧ウィンドウも追加しました。
- **表示レイアウトの調整機能**: WBS表とガントチャートの境界、タスク名列・コメント列の幅をドラッグで調整できるようにしました。
- **参照JSONの復元・ダウンロード**: 生成HTMLに埋め込まれた元のJSONを、CLI（`--restore-source`）または画面上のリンクから取り出せるようにしました。
- **XLSXエクスポート**: `export xlsx`コマンドで、WBS・ガントの日付ヘッダー・休日表示・計画/進捗/実績マークを含むExcel（Google Sheetsでも利用可）ファイルを出力できるようにしました。
- **サブコマンド体系のCLI**: `init`（雛形生成）、`template`（編集用スケルトン生成）、`validate`（検証、`--json`での機械可読出力に対応）、`render`（HTML生成）、`restore-source`、`export xlsx`に加え、`task add` / `task update` / `task show` / `task remove` / `project update`による入力JSONの追加・更新・参照・削除コマンドを整備しました。
- **配布形式**: 単一ファイル実行に加えて、パッケージ構成（`python3 -m wbsgen`）や自己完結のzipapp（`wbsgen.pyz`）としても実行でき、GitHub Releasesからzipappと利用者マニュアル（`wbsgen-manual.html`）を配布する運用を整えました。
- **利用者マニュアル**: 基本ワークフロー、コマンドリファレンス、画面の見方、生成HTML上の操作方法などをまとめた`MANUAL.html`（単体で開けるローカル閲覧用マニュアル）を追加しました。

### 改善

- 警告一覧を、初期表示で開く右下固定の非モーダルウィンドウに変更し、閲覧の邪魔になりにくくしました。
- 生成HTMLの行高・バー・配色・密度などの視覚表現を、合意済みのデザイン基準に合わせて調整しました。
- イナズマ線の進捗点の接続ロジックとガント右端の余白を改善しました。
- 全CLIサブコマンドのヘルプを、目的・入力・制約が分かりやすい内容に整備しました。
- 入出力仕様ドキュメント（`docs/data-format.md`）に、検証エラー・警告コード29種の対応表を追加しました。
- 利用者マニュアルの配色・目次構成・コマンドリファレンスをカード形式に刷新し、視認性を高めました。

### 内部改善

- 単一ファイル実装を責務別のpackage構成へ分割し、WBS表・ガントの幅計算ロジックをモジュール化してテスト可能にするなど、内部構造のリファクタリングを行いました。
- README・入出力仕様・開発ルール文書（AI_RULES.md、CLAUDE.md、各種開発スキル）の構成整理を継続的に行いました。
